export enum IncreaseEvery {
    MONTHLY = 'M',
    YEARLY = 'Y'
}

export enum Gender {
    MALE = 'M',
    FEMALE = 'F'
}

export enum Frequency {
    HOURLY = 1,
    DAILY = 2,
    WEEKLY = 3,
    FORTNIGHTLY = 4,
    MONTHLY = 5,
    YEARLY = 6
}

export enum EmploymentType {
    CASUAL = 1,
    PART_TIME = 2,
    FULL_TIME = 3
}

export enum ProjectType {
    MILESTONE_BASE = 1,
    TIME_BASE = 2
}